# Cours 2

## Paramétrisation

$\Omega \subset \mathbb{R}^2 \to \mathcal{S} \subset \mathbb{R}^3$

On a $P : \Omega \to \mathcal{S}$ et $U : \mathcal{S} \to \Omega$, avec $p(u; v) = \begin{bmatrix}
x(u; v) \\
y(u; v) \\
z(u; v)
\end{bmatrix}$

En 2D ($z = f(u; v)$), le grad (qui est un vec) est $\perp$ aux iso.
La pente ds une certaine dir, c le prod scal avec le grad.

## Opérateurs différentiels

Le noyau de l'opérateur laplacien s'appelle les fn harmoniques : $\Delta f = 0$.

$\sin$ et $\cos$ sont des vecteurs propres de l'opérateur laplacien, tout comme en fait en général $x \mapsto \cos(\lambda x + Cte)$

Rappel Nabla : $\nabla =  \begin{bmatrix}
\partial_u \\
\partial_v
\end{bmatrix}$, comme ça

- le grad d'une fn scal c $\nabla f$
- la divergence d'un vec c $\nabla \cdot v$
- le laplacien d'une fn scal c $\Delta f = (\nabla \cdot \nabla) f$
- et le rotationnel d'un vec c c $\nabla \times v$

### Une courbe ds $\mathbb{R}^3$

$x : [0; L] \to \mathbb{R}^3$ de classe $\mathcal{C}^3$

- tangent vec : $t(t) = \dfrac{X'(t)}{||X'(t)||}$
- normal vec : $m(t) = \dfrac{t'(t)}{||t'(t)||}$
- binormal vec : $b(t) = $

### D'une surface ds $\mathbb{R}^2$ à une ds $\mathbb{R}^3$

C le cours de L3 mdrr

$X : \Omega \subset \mathbb{R}^2 \to \mathcal{S} \subset \mathbb{R}^3$

On pose $X_u = \tfrac{\partial X}{\partial u}$ et $X_v = \tfrac{\partial X}{\partial v}$.

- Plan tangent en $p$ :
- vec normal : $n = \dfrac{\times}{}$

Les 3 formes fondamentales :

- $I = \begin{bmatrix}
E & F \\
F & G
\end{bmatrix} = \begin{bmatrix}
x_u^T x_u & x_u^T x_v \\
x_v^T x_u & x_v^T x_v
\end{bmatrix}
$
- $II = \begin{bmatrix}
e & f \\
f & g
\end{bmatrix} = \begin{bmatrix}
x_{uu}^T n & x_{uv}^T n \\
x_{vu}^T n & x_{vv}^T n
\end{bmatrix}
$

$dA = \sqrt{EG - F^2} du dv$

...

$$\nabla u = \dfrac{1}{2 A_f} \sum_i u_i (N \times e_i)$$
où $N$ c la normale au triangle et $e_i$ c des arêtes.

$$\Delta u = \dfrac{1}{2A_i} \sum_{j \in voisins(i)} (\cot \alpha_{ij} + \cot \alpha_{ji}) (u_j - u_i)$$
où $A_i$ c l'aire autour du vertex $i$

Propag de la chaleur : $\dfrac{\partial u}{\partial t} = \Delta u$

$\Delta_x u = -2Hn$ où $H$ c la courbure moyenne et $n$ c le vec normal

### Laplaciens décrits comme matrices carrées cd taille $n_v = $ taille du vecteur $U$

- Diagonale : $$L_{ii} = \dfrac{-1}{2 A_i} \sum_\text{voisins j} (\cot \alpha_{ij} + \cot \alpha_{ji})$$
- Si $i$ et $j$ sont voisins : $$L_{ii} = \dfrac{+1}{2 A_i} (\cot \alpha_{ij} + \cot \alpha_{ji})$$
- Sinon : $$L_{ii} = 0$$

## ?

$u_i$ température du sommet $i$

$\Delta u = \dfrac{u_i^{t + \Delta t} - u_i}{\Delta t}$

$u_i^{t + \Delta t} - u_i = \alpha (\Delta u^t)_i$
